<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Paleta de cores baseada no protótipo (dark + azul/roxo) -->
    <SolidColorBrush x:Key="BgPrincipal" Color="#0B0F1A"/>
    <SolidColorBrush x:Key="BgSidebar" Color="#0E1424"/>
    <SolidColorBrush x:Key="BgCard" Color="#131A2B"/>
    <SolidColorBrush x:Key="BorderCard" Color="#232A42"/>
    <SolidColorBrush x:Key="TextoPrimario" Color="#E5E9F0"/>
    <SolidColorBrush x:Key="TextoSecundario" Color="#8EA0C9"/>
    <SolidColorBrush x:Key="AzulDestaque" Color="#5B8DEF"/>
    <SolidColorBrush x:Key="RoxoDestaque" Color="#8B5CF6"/>
    <SolidColorBrush x:Key="VerdeOk" Color="#3DDC84"/>
    <SolidColorBrush x:Key="AmareloAlerta" Color="#F5A623"/>
    <SolidColorBrush x:Key="VermelhoErro" Color="#F25555"/>
    <LinearGradientBrush x:Key="ScrollThumbGradiente" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Offset="0" Color="#5B8DEF"/>
        <GradientStop Offset="1" Color="#8B5CF6"/>
    </LinearGradientBrush>

    <Style x:Key="CardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource BgCard}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderCard}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="20"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="18" ShadowDepth="0" Opacity="0.25" Color="#000000"/>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource AzulDestaque}"/>
                <Setter Property="Background" Value="#19223A"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="TituloCard" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextoPrimario}"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>

    <Style x:Key="LabelSecundario" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextoSecundario}"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

    <Style x:Key="BotaoPrimario" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource AzulDestaque}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="16,10"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="BotaoSecundario" TargetType="Button" BasedOn="{StaticResource BotaoPrimario}">
        <Setter Property="Background" Value="{StaticResource BgCard}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderCard}"/>
        <Setter Property="Foreground" Value="{StaticResource TextoPrimario}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="12,8">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#1A2440"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AzulDestaque}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ItemMenu" TargetType="RadioButton">
        <Setter Property="Foreground" Value="{StaticResource TextoSecundario}"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="border" Background="Transparent" BorderBrush="Transparent" BorderThickness="1" CornerRadius="8" Padding="{TemplateBinding Padding}">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#1A2440"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AzulDestaque}"/>
                            <Setter Property="Foreground" Value="{StaticResource AzulDestaque}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#141B30"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource RoxoDestaque}"/>
                            <Setter Property="Foreground" Value="{StaticResource TextoPrimario}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ScrollBar">
        <Setter Property="Width" Value="12"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="Transparent">
                        <Border Background="#0D1220" CornerRadius="6" Margin="2"/>
                        <Track x:Name="PART_Track" IsDirectionReversed="True" Margin="2">
                            <Track.Thumb>
                                <Thumb Cursor="Hand">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border x:Name="ThumbBorder"
                                                    CornerRadius="6"
                                                    Background="{StaticResource ScrollThumbGradiente}"
                                                    Opacity="0.75"
                                                    RenderTransformOrigin="0.5,0.5">
                                                <Border.RenderTransform>
                                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                                </Border.RenderTransform>
                                                <Border.Effect>
                                                    <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.45" Color="#5B8DEF"/>
                                                </Border.Effect>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Trigger.EnterActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <DoubleAnimation Storyboard.TargetName="ThumbBorder" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                                                <DoubleAnimation Storyboard.TargetName="ThumbBorder" Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1.12" Duration="0:0:0.15"/>
                                                                <DoubleAnimation Storyboard.TargetName="ThumbBorder" Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1.12" Duration="0:0:0.15"/>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </Trigger.EnterActions>
                                                </Trigger>
                                                <Trigger Property="IsDragging" Value="True">
                                                    <Setter TargetName="ThumbBorder" Property="Background" Value="{StaticResource VerdeOk}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
